#!/bin/sh
# Maven Wrapper script for Unix/Linux/macOS
# Generated by Spring Initializr — do not edit manually.
# See .mvn/wrapper/maven-wrapper.properties for the Maven version.

set -e

MAVEN_WRAPPER_JAR=".mvn/wrapper/maven-wrapper.jar"
MAVEN_WRAPPER_PROPERTIES=".mvn/wrapper/maven-wrapper.properties"

# Determine JAVA_HOME
if [ -n "$JAVA_HOME" ]; then
    JAVACMD="$JAVA_HOME/bin/java"
else
    JAVACMD="java"
fi

# Download wrapper jar if not present
if [ ! -f "$MAVEN_WRAPPER_JAR" ]; then
    WRAPPER_URL=$(grep "wrapperUrl" "$MAVEN_WRAPPER_PROPERTIES" | cut -d= -f2-)
    echo "Downloading Maven Wrapper from $WRAPPER_URL"
    if command -v curl > /dev/null; then
        curl -o "$MAVEN_WRAPPER_JAR" "$WRAPPER_URL" -s
    elif command -v wget > /dev/null; then
        wget -q -O "$MAVEN_WRAPPER_JAR" "$WRAPPER_URL"
    else
        echo "ERROR: Neither curl nor wget is available. Cannot download Maven Wrapper." >&2
        exit 1
    fi
fi

exec "$JAVACMD" \
    -classpath "$MAVEN_WRAPPER_JAR" \
    "-Dmaven.multiModuleProjectDirectory=$PWD" \
    org.apache.maven.wrapper.MavenWrapperMain "$@"
